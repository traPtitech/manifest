apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: gitea-dev-storage
  name: gitea-dev-storage
spec:
  resources:
    requests:
      storage: 100Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
