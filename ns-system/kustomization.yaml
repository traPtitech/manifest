helmCharts:
  - repo: oci://ghcr.io/traptitech/charts
    name: neoshowcase
    version: "1.9.5"
    includeCRDs: true
    namespace: ns-system
    releaseName: ns
    valuesFile: values.yaml

resources:
  - db
  - backup
  - secrets
  - app-namespace.yaml
  - certificate.yaml
