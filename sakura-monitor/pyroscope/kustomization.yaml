namespace: monitor

helmCharts:
  - repo: https://grafana.github.io/helm-charts
    name: pyroscope
    version: "2.1.2"
    includeCRDs: true
    namespace: monitor
    releaseName: pyroscope
    valuesFile: values.yaml

generators:
  - ksops.yaml
