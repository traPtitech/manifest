apiVersion: v1
kind: Service
metadata:
  name: jomon
spec:
  selector:
    app.kubernetes.io/name: jomon
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
