apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: jomon-tls
spec:
  secretName: jomon-tls
  dnsNames:
    - jomon.trap.jp
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
