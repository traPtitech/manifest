apiVersion: v1
kind: Service
metadata:
  name: jomon-backend
spec:
  selector:
    app.kubernetes.io/name: jomon-backend
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
