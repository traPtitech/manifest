apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: test-fail
  generateName: test-fail-

spec:
  entrypoint: intentional-fail
  templates:
    - name: intentional-fail
      container:
        # renovate:image-full
        image: "docker.io/alpine:20230901"
        command: [sh, -c]
        args: ["echo intentional failure; exit 1"]
