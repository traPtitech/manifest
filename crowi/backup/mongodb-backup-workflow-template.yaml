apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: crowi-mongodb-backup
  generateName: crowi-mongodb-backup-

spec:
  entrypoint: run
  serviceAccountName: argo-workflow
  templates:
    - name: run
      volumes:
        - name: scripts
          configMap:
            name: crowi-backup-scripts
            defaultMode: 0755
        - name: keys
          secret:
            secretName: backup-keys
      container:
        name: backup
        image: gcr.io/google.com/cloudsdktool/google-cloud-cli:alpine
        imagePullPolicy: Always
        command:
          - /scripts/backup-mongodb.sh
        volumeMounts:
          - mountPath: /scripts
            name: scripts
          - mountPath: /keys
            name: keys
        env:
          - name: DB_HOST
            value: crowi-mongo.crowi.svc.cluster.local
          - name: DB_PORT
            value: "27017"
          - name: DB_NAME
            value: crowi
          - name: BACKUP_FILE
            value: crowi-mongo-backup.gz
