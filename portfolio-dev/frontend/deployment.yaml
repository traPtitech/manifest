apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: portfolio-dev-frontend
  name: portfolio-dev-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio-dev-frontend
  template:
    metadata:
      labels:
        app: portfolio-dev-frontend
    spec:
      nodeSelector:
        kubernetes.io/hostname: eee101.tokyotech.org
      containers:
        - image: ghcr.io/traptitech/traportfolio-ui:main
          name: portfolio-dev-frontend
          ports:
            - containerPort: 80
              protocol: TCP
      restartPolicy: Always
