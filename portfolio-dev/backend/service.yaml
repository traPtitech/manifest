apiVersion: v1
kind: Service
metadata:
  labels:
    app: portfolio-backend
  name: portfolio-backend
spec:
  ports:
    - name: "3009"
      port: 3009
      targetPort: 1323
  selector:
    app: portfolio-backend
