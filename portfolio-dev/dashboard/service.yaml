apiVersion: v1
kind: Service
metadata:
  labels:
    app: portfolio-dashboard
  name: portfolio-dashboard
spec:
  ports:
    - name: "3015"
      port: 3015
      targetPort: 80
  selector:
    app: portfolio-dashboard
