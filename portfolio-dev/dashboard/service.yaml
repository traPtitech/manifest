apiVersion: v1
kind: Service
metadata:
  labels:
    app: portfolio-dev-dashboard
  name: portfolio-dashboard
spec:
  ports:
    - name: "3015"
      port: 3015
      targetPort: 80
  selector:
    app: portfolio-dev-dashboard
