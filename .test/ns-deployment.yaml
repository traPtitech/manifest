apiVersion: apps/v1
kind: Deployment
metadata:
  name: ns-test
spec:
  selector:
    matchLabels:
      app: ns-test
  template:
    metadata:
      labels:
        app: ns-test
    spec:
      containers:
        - name: ns-test
          image: ghcr.io/traptitech/ns-dashboard:1.9.4
        - name: ns-test-2
          image: ghcr.io/traptitech/ns-gateway:1.9.3
