namePrefix: sakura-

resources:
  - ../dev-ops-bot/

patches:
  - patch: |-
      - op: replace
        path: /metadata/name
        value: sakura-dev-ops-bot-view
      - op: replace
        path: /subjects/0/name
        value: sakura-dev-ops-bot
      - op: replace
        path: /subjects/0/namespace
        value: sakura-dev-ops-bot
    target:
      kind: ClusterRoleBinding
      name: dev-ops-bot-view
  - patch: |-
      - op: replace
        path: /metadata/name
        value: sakura-dev-ops-bot-extra
      - op: replace
        path: /subjects/0/name
        value: sakura-dev-ops-bot
      - op: replace
        path: /subjects/0/namespace
        value: sakura-dev-ops-bot
    target:
      kind: ClusterRoleBinding
      name: dev-ops-bot-extra

configMapGenerator:
  - name: config
    files:
      - config/config.yaml
      - config/entrypoint.sh
      - config/known_hosts
