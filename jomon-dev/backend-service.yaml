apiVersion: v1
kind: Service
metadata:
  name: jomon-dev-backend
spec:
  selector:
    app.kubernetes.io/name: jomon-dev-backend
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
