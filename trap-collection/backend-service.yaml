apiVersion: v1
kind: Service
metadata:
  name: trap-collection-backend
spec:
  selector:
    app.kubernetes.io/name: trap-collection-backend
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
