apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo

spec:
  replicas: 0
  template:
    spec:
      nodeSelector:
        kubernetes.io/hostname: eee101.tokyotech.org
      containers:
        - name: mongo
          resources:
            limits:
              memory: "1G"
