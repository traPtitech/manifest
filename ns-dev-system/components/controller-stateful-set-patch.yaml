apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ns-controller

spec:
  template:
    spec:
      nodeSelector:
        kubernetes.io/hostname: eee101.tokyotech.org

      containers:
        - name: ns-controller
          image: ns-controller-dev
          imagePullPolicy: Always
          resources:
            requests:
              cpu: "50m"
              memory: "100Mi"
            limits:
              cpu: "1"
              memory: "150Mi"
