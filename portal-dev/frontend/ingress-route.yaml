apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: portal-dev-frontend

spec:
  entryPoints:
    - websecure
  tls:
    secretName: trapti-tech-tls
  routes:
    - kind: Rule
      match: Host(`portal-dev.trapti.tech`)
      services:
        - namespace: portal-dev
          kind: Service
          name: portal-dev-frontend
          port: 8100
          scheme: http
          strategy: RoundRobin
          weight: 1
