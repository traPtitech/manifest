apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: knoq-certificate

spec:
  issuerRef:
    kind: ClusterIssuer
    name: cluster-issuer
  secretName: knoq-dev-tls
  duration: 2160h0m0s # 90d
  renewBefore: 720h0m0s # 30d
  dnsNames:
    - knoq-dev.trapti.tech
