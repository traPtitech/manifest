namespace: monitor

helmCharts:
  - repo: https://grafana.github.io/helm-charts
    name: pyroscope
    version: "2.0.1"
    includeCRDs: true
    namespace: monitor
    releaseName: pyroscope
    valuesFile: values.yaml

generators:
  - ksops.yaml
