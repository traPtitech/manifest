apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: knoq-tls
spec:
  secretName: knoq-tls
  dnsNames:
    - knoq.trap.jp
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
