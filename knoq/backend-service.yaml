apiVersion: v1
kind: Service
metadata:
  name: knoq-backend
spec:
  selector:
    app.kubernetes.io/name: knoq-backend
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
