apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: knoq
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`knoq.trap.jp`) && PathPrefix(`/api`)
      kind: Rule
      services:
        - name: knoq-backend
          port: 3000
    - match: Host(`knoq.trap.jp`)
      kind: Rule
      services:
        - name: knoq-frontend
          port: 80
  tls:
    secretName: knoq-tls
