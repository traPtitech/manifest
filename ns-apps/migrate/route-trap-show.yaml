apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: migrate-route-trap-show

spec:
  entryPoints:
    - websecure
  tls:
    secretName: nsapp-trap-show-tls
  routes:
    - kind: Rule
      match: Host(`trap.show`)
      priority: 1
      services:
        - kind: TraefikService
          name: h101-secure@file
