resources:
  - es-service.yaml
  - es-statefulset.yaml
  - system-bot-deployment.yaml

configMapGenerator:
  - name: es-config
    files:
      - config/es_jvm.options
  - name: traq-config
    files:
      - config/traq.yaml

generators:
  - ksops.yaml
